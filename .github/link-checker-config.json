{
    "ignorePatterns": [
        {
            "pattern": "^(http|https)://localhost"
        },
        {
            "pattern": "mailto:{{.*}}"
        },
        {
            "pattern": "https://github.com/{{.*}}/actions/runs/{{.*}}"
        },
        {
            "pattern": "(WWW.INFO|www.info)"
        }
    ],
    "replacementPatterns": [
        {
            "pattern": "^/",
            "replacement": "{{BASEURL}}/"
        }
    ],
    "retryOn429": true,
    "aliveStatusCodes": [
        200,
        206
    ]
}